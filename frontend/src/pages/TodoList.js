import React from 'react';
import { Layout } from '../layout';

const TodoList = () => {
    return (
        <Layout>
            This is the TodoList page
        </Layout>
    )
};

export default TodoList;